# Lubrifiants App - Static Netlify-ready version

This version uses a static frontend (no build). Deploy on Netlify and set SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY environment variables.

Publish directory: frontend
Build command: (leave empty)
